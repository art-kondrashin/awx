[
{% for host in ansible_play_hosts_all %}
  {
    "targets": ["{{ (hostvars[host].ansible_host | default(host)) }}:9399"],
    "labels": {
      "ip": "{{ (hostvars[host].ansible_host | default(host)) }}",
      "hostname": "{{ host.hostname }}"
    }
  }{% if not loop.last %},{% endif %}
{% endfor %}
]
