[
{% for h in hosts %}
  {
    "targets": ["{{ h.ip }}:{{ port }}"],
    "labels": {
      "ip": "{{ h.ip }}",
      "hostname": "{{ h.hostname }}",
      "job": "{{ job }}"
    }
  }{% if not loop.last %},{% endif %}
{% endfor %}
]
